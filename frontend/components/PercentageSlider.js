import React, { Component } from 'react';
import { Block, Text } from '.';
import { theme } from '../constants';

export default class PercentageSlider extends Component {
  render() {
    return (
        <Block>
            <Text> slider component, yess </Text>
        </Block>
    );
  }
}

